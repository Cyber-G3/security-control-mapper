const sectors={general:{label:'General',adjustment:0,priority:[]},'saas-technology':{label:'SaaS / Technology',adjustment:5,priority:['repository-security','vulnerability-management','secrets']},'cloud-provider':{label:'Cloud Provider',adjustment:10,priority:['privileged-access','logging-monitoring','backup-recovery']},'financial-services':{label:'Financial Services',adjustment:10,priority:['privileged-access','third-party-risk','incident-response']},'healthcare-life-sciences':{label:'Healthcare / Life Sciences',adjustment:10,priority:['identity-access','backup-recovery','third-party-risk']},'public-sector':{label:'Public Sector',adjustment:8,priority:['identity-access','logging-monitoring','incident-response']},'critical-infrastructure':{label:'Critical Infrastructure',adjustment:15,priority:['backup-recovery','incident-response','third-party-risk']},'manufacturing-industrial':{label:'Manufacturing / Industrial',adjustment:8,priority:['vulnerability-management','backup-recovery','third-party-risk']},'professional-services':{label:'Professional Services',adjustment:3,priority:['identity-access','third-party-risk','secrets']}};
const severityScore={LOW:10,MEDIUM:30,HIGH:55,CRITICAL:75},assetScore={LOW:0,MEDIUM:8,HIGH:15,CRITICAL:22},dataScore={PUBLIC:0,INTERNAL:4,CONFIDENTIAL:9,RESTRICTED:14},serviceScore={LOW:0,MEDIUM:5,HIGH:10,CRITICAL:15},priorityRank={CRITICAL:4,HIGH:3,MEDIUM:2,LOW:1};
let findings=[];

const sector=document.querySelector('#sector'),framework=document.querySelector('#framework'),priority=document.querySelector('#priority'),domain=document.querySelector('#domain'),asset=document.querySelector('#asset'),data=document.querySelector('#data'),service=document.querySelector('#service'),internet=document.querySelector('#internet'),active=document.querySelector('#active'),compensating=document.querySelector('#compensating'),search=document.querySelector('#search'),portfolio=document.querySelector('#portfolio'),detail=document.querySelector('#detail');

async function loadDatasets(){
  const [catalogResponse,mappingResponse]=await Promise.all([fetch('data/findings-v0.2.json'),fetch('data/mappings-v0.1.json')]);
  if(!catalogResponse.ok||!mappingResponse.ok)throw new Error('Unable to load versioned Security Control Mapper datasets.');
  const catalog=await catalogResponse.json(),mappings=await mappingResponse.json();
  const mappingByType=new Map(mappings.records.map(record=>[record.finding_type,record]));
  findings=catalog.findings.map(item=>{
    const mapping=mappingByType.get(item.finding_type);
    return {...item,severity:item.default_severity,evidence:item.evidence_expected,refs:(mapping?.references||[]).map(ref=>[ref.framework,ref.reference,ref.title,ref.confidence]),mappingVersion:mappings.mapping_version,catalogVersion:catalog.catalog_version};
  });
}

function contextualRisk(finding){
  const selectedSector=sectors[sector.value];let score=severityScore[finding.severity]+assetScore[asset.value]+dataScore[data.value]+serviceScore[service.value]+selectedSector.adjustment;const reasons=[`severity:${finding.severity.toLowerCase()}`,`sector:${sector.value}`];
  if(selectedSector.priority.includes(finding.domain)){score+=7;reasons.push('sector_priority_domain');}
  if(internet.checked){score+=12;reasons.push('internet_exposed');}if(active.checked){score+=20;reasons.push('active_exploitation');}if(compensating.checked){score-=12;reasons.push('compensating_controls');}
  score=Math.max(0,Math.min(100,score));const riskPriority=score>=85?'CRITICAL':score>=65?'HIGH':score>=40?'MEDIUM':'LOW';return {score,priority:riskPriority,reasons};
}

function filteredPortfolio(){const q=search.value.trim().toLowerCase();return findings.map(f=>({...f,risk:contextualRisk(f)})).filter(f=>{if(priority.value&&f.risk.priority!==priority.value)return false;if(domain.value&&f.domain!==domain.value)return false;if(framework.value&&!f.refs.some(r=>r[0]===framework.value))return false;if(q&&!`${f.title} ${f.domain} ${f.evidence.join(' ')}`.toLowerCase().includes(q))return false;return true;}).sort((a,b)=>priorityRank[b.risk.priority]-priorityRank[a.risk.priority]||b.risk.score-a.risk.score||a.title.localeCompare(b.title));}

function render(){const items=filteredPortfolio();document.querySelector('#kpi-total').textContent=items.length;document.querySelector('#kpi-critical').textContent=items.filter(x=>x.risk.priority==='CRITICAL').length;document.querySelector('#kpi-high').textContent=items.filter(x=>x.risk.priority==='HIGH').length;document.querySelector('#kpi-average').textContent=items.length?Math.round(items.reduce((s,x)=>s+x.risk.score,0)/items.length):0;portfolio.innerHTML=items.map((f,i)=>`<button class="finding-row" data-index="${i}"><span><strong>${f.title}</strong><small>${f.finding_id} · ${f.domain}</small></span><span class="risk ${f.risk.priority.toLowerCase()}">${f.risk.priority}<b>${f.risk.score}</b></span></button>`).join('')||'<p class="notice">No findings match the current filters.</p>';portfolio.querySelectorAll('.finding-row').forEach(el=>el.addEventListener('click',()=>renderDetail(items[Number(el.dataset.index)])));if(items.length)renderDetail(items[0]);else detail.innerHTML='';}

function renderDetail(f){detail.innerHTML=`<div class="detail-head"><div><h3>${f.title}</h3><p class="notice">${f.finding_id} · ${f.domain} · catalog ${f.catalogVersion}</p></div><span class="risk ${f.risk.priority.toLowerCase()}">${f.risk.priority}<b>${f.risk.score}</b></span></div><div class="detail-grid"><article><h4>Why prioritized</h4><ul>${f.risk.reasons.map(x=>`<li>${x}</li>`).join('')}</ul></article><article><h4>Remediation</h4><p>${f.remediation}</p></article><article><h4>Evidence expected</h4><ul>${f.evidence.map(x=>`<li>${x}</li>`).join('')}</ul></article></div><h4>Supporting framework references</h4><div class="refs">${f.refs.filter(r=>!framework.value||r[0]===framework.value).map(r=>`<article class="ref"><strong>${r[0]} · ${r[1]}</strong><span>${r[2]}</span><em>${r[3]}</em></article>`).join('')||'<p class="notice">No versioned mapping is available yet for this finding/framework.</p>'}</div><p class="notice">Risk is contextual prioritization. Framework mappings indicate supporting relevance only and do not establish legal applicability, compliance or certification.</p>`;}

function configureFilters(){Object.entries(sectors).forEach(([value,item])=>sector.add(new Option(item.label,value)));[...new Set(findings.map(f=>f.domain))].sort().forEach(value=>domain.add(new Option(value.replaceAll('-',' '),value)));const frameworks=[...new Set(findings.flatMap(f=>f.refs.map(r=>r[0])))].sort();frameworks.forEach(value=>{if(![...framework.options].some(o=>o.value===value))framework.add(new Option(value,value));});[sector,framework,priority,domain,asset,data,service,internet,active,compensating,search].forEach(el=>el.addEventListener(el===search?'input':'change',render));sector.value='general';}

(async()=>{try{await loadDatasets();configureFilters();render();}catch(error){portfolio.innerHTML=`<p class="notice">${error.message}</p>`;console.error(error);}})();
